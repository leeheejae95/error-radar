spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

jwt:
  secret: "test-secret-key-must-be-at-least-32-bytes-long-for-hs256"

alert:
  threshold:
    count: 3
    window-minutes: 30
  slack:
    webhook-url: "http://localhost/test-webhook"
